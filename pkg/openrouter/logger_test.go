,
	})trics: true		EnableMenfo,
velILogLeel:         ig{
		LevConfr(LoggerNewLoggeogger := ) {
	ling.Ttests(t *etric_LogM TestLogger
func
	}
}
 output)got: %s",output, 200 in status code ("Expected rrorf	t.E0") {
	tput, "20ouins(s.Contaf !string	}

	itput)
", ou: %s output, gotinI Response' d 'APExpectet.Errorf("
		{Response") "API tput, tains(ouon!strings.C
	if 	})
 duration)
ponseBody,headers, ressponse(200, ogRegger.L {
		loutput(func()LogOure:= captt utpu

	oisecondMill 500 * time.ation :=	dur


	}ce{}{}, []interfa"choices":
		mpletion","chat.coject":  		"ob",
hatcmpl-123":      "cd
		"irface{}{ng]intemap[striody := sponseB

	rejson")ation/, "applicntent-Type".Set("Co
	headerseader{}p.Hders := htt)

	hea	},
g: truenseLoespo	EnableR	 true,
estLog: nableRequebug,
		E   LogLevelD          		Level:nfig{
gerCoger(Log := NewLog	logger) {
.Te(t *testingsponsogReger_L TestLognc}
}

fu	ut)
, outps"ted, got: %dac reder to ben hearizatiopected authorrorf("Ex		t.E) {
ACTED]""[REDs(output, .Containtrings !s	}

	if, output)
%s"tput, got: T' in oud 'POS"Expecterorf(
		t.ErOST") { "Pput,s(outtainings.Constr !	if
	}

", output): %sotin output, gt'  Requesd 'APIcterrorf("Expet.E		est") {
"API Requutput, ns(oContairings.if !st
	
	})
dy)uestBoreqreq, est(Requer.Log		logg
) {nc((fueLogOutputuraptut := c

	outp	},
	}"},
	"Hellotent": ", "cone": "user	{"roling{
		tring]strs": []map[sssage
		"mept-4",": "g		"modelace{}{
erfp[string]int := ma	requestBody

json")ication/"applt-Type", en"Contader.Set(	req.He")
-keyk-testarer sBen", "orizatio"Authet(.Seq.Header)
	rpt-4"}`)l":"g"moder(`{NewReaderings. stetions",1/chat/complr.ai/api/v/openroutettps:/POST", "hewRequest(" httptest.N
	req :=})
,
	rueog: tResponseL	Enable true,
	stLog: EnableRequebug,
		  LogLevelDe:           	Levelig{
	nf(LoggerCoerr := NewLogg
	loggeing.T) {estRequest(t *tr_Log TestLogge
func}

	}

		})		}	t)
, outpu got: %s" buttput ouected no logErrorf("Exp
				t."" { && output !=g tt.shouldLo		if !		}
		ne")
t noutput but go log o("Expectedt.Errorf
				 "" {put ==dLog && outoulif tt.sh})

			
			ogger)on(lunctit.logF			tc() {
	utput(funtureLogO := captput			ou})

e,
			trusponseLog: 		EnableRe		:  true,
leRequestLog	Enab	
		ue,cs:     trEnableMetri			el,
	t.logLev    t         Level:				erConfig{
Logger(Loggogger := New
			lng.T) {sti(t *teunct.name, f	t.Run(t{
	nge tests  tt := ra
	for _,		},
	}
: false,
houldLog},
			s	")
		ge messafofo("test inIn		l.{
		gger) c(l *Loion: fun	logFunct	,
	ogLevelErrorl: LLeve			log
essages",nfo mkips i level sor   "Errname:  	{
				},
	 true,
		shouldLog:		},
		)
	ge"messar test erro("Error	l.			er) {
(l *Logg funcction:ogFunor,
			lvelErrgLel: LogLeve		lo	ges",
ror messa erl logs"Error leveame:     	{
			n
		},
	e,og: truouldL
			sh,		}	")
messageinfo nfo("test 	l.I
			r) {gge *Lonc(ltion: fuogFunc	lnfo,
		velIogLeLevel: L	loges",
		 messag logs info"Info level   			name:  	{
	
		},
e,Log: falsuld			sho		},
	
age")bug messtest del.Debug("		er) {
		ggnc(l *Lo function:		logFuInfo,
	gLevelLevel: Lo	log
		",messagesskips debug vel    "Info lee:  	nam
		
		{		},
dLog: true,		shoul	},
			sage")
es debug mDebug("test	l.	{
		*Logger) func(l Function: 	log
		g,velDebuvel: LogLegLe			los",
g messagegs debul log leve"Debu     	name:
		{
		}{ bool
	houldLog  		sLogger)
ion func(*gFunct	loLevel
	l    Log	logLevering
	  stame      uct {
		n[]strsts :=  {
	teng.T)estils(t *tr_LogLevestLoggeunc Teg()
}

ff.Strinrn buretufn()
	
			
s.Stdout)
(oputog.SetOutefer l
	d(&buf)Outputg.Setuffer
	lotes.Bbuf byar {
	v)) string unc(t(fn feLogOutputurcapunc ing
fr testutput foog oaptures leLogOutput c
// capturme"
)

	"tisting"	"terings"
	"stos"
t"
	"tp/httptes	"net/htp"
net/httg"
	"	"lo"
onng/jscodi"enontext"
		"cs"
	"bytert (


imporge openroutekapac